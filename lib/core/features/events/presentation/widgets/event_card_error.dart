import 'package:flutter/material.dart';

class EventCardError extends StatelessWidget {
  const EventCardError({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox.shrink();
  }
}
